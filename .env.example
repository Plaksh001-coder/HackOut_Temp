# GreenMind Environment Configuration

# Server
PORT=8000
HOST=127.0.0.1

# Optional External LLM Integration (Gracefully falls back to deterministic mock assistant if absent)
LLM_API_KEY=
LLM_PROVIDER=gemini

# Default Database (Default in-memory demo data is active for HackOut'26)
DATABASE_URL=
